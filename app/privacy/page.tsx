export default function PrivacyPage() { return <main className="privacy-page"><a href="/" className="back-link">← Back to signal/ats</a><span className="eyebrow">Privacy notes</span><h1>Your documents stay yours.</h1><p>signal/ats is designed as a local-first resume comparison tool. The analyzer does not use cookies, accounts, analytics on document text, browser storage, or a database.</p><h2>What happens when you analyze</h2><p>Your browser sends the two text fields to the same app&apos;s analysis endpoint so the deterministic analysis can run. The content is processed in memory for the request and is not intentionally persisted. Rate-limit counters may exist in process memory, but they do not contain document content.</p><h2>What the score means</h2><p>This is a directional editing aid, not a promise of hiring outcomes. Matching uses conservative normalization and cannot understand every ATS implementation. Review every suggestion and only add claims you can support.</p></main> }
